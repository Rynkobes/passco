const request = require('supertest')
const app

describe('Test GET /login', () => {
    test('it should redirect to auth0', () => {
        
    })

    test('It should respond with 200 success', () => {
        
    })

    test('It should return something that will', () => {
        
    })
})